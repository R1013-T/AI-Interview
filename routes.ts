export const publicRoutes: string[] = ['guest']

export const authRoutes: string[] = ['/auth', '/auth/sign-up', '/auth/sign-in']

export const apiAuthPrefix: string = '/api/auth'

export const DEFAULT_LOGIN_REDIRECT: string = '/dashboard'
